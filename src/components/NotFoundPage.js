import React from 'react';

export const NotFoundPage = () => {
    <div>
        <h1>Oops, you have reached this page in error!</h1>
        <button>Return Home</button>
    </div>
};

export default NotFoundPage;